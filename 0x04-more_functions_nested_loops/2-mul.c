#include <stdlib.h>
#include <stdio.h>
#include <ctype.h>

/* more headers goes there */

/**
 * mul- multiply 2 ints
 *@a : int to be multiplied
 *@b : int to be multiplied
 *
 * Return: Always 0 (Success)
 */
int mul(int a, int b);
int mul(int a, int b)
{
	return (a * b);
}
