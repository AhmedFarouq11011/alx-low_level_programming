#include <stdio.h>
#include "main.h"
#include <string.h>
/**
 *_puts  - prints the length of a string
 *@str : Pointer that will be updated
 *
 * Return: Always 0.
 */
void _puts(char *str);
void _puts(char *str)
{
	puts(str);
}
