#include <stdio.h>
#include <stdlib.h>
#include "main.h"
#include <string.h>
/**
 *_atoi  - prints the length of a string
 *@s: string variable
 *
 * Return: Always 0.
 */
int _atoi(char *s);
int _atoi(char *s)
{
	return (atoi(s));
}
