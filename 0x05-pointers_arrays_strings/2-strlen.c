#include <stdio.h>
#include "main.h"
#include <string.h>
/**
 *_strlen  - prints the length of a string
 *@s : Pointer that will be updated
 *
 * Return: Always 0.
 */
int _strlen(char *s);
int _strlen(char *s)
{
	int x = strlen(s);

	return (x);
}
