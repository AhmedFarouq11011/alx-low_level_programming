#include <stdio.h>
#include <stdlib.h>
#include "main.h"
#include <ctype.h>
/**
 *add  - Print 1 if n > 0 elif n < 0 return -1 else 0
 *@x : Description of parameter y
 *@y : Description of paramter x
 *
 * Return: Always 0.
 */
int add(int x, int y);
int add(int x, int y)
{
	return (x + y);
}
